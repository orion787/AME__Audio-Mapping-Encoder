# 𝄞 AME (Audio Mapping Encoder) 𝄞
## Этот проект является дипломной работой, выполненной в исследовательских целях.
## Распространяется под лицензией GNU GPL V3.

Данный проект выполняет генерацию ID (уникальных идентификаторов) и паролей из аудиофайлов форматов mp3, flac, wav. Работает только с файлами, находящимися непосредственно на компьютере. Однако, если Вам будет необходима функция обработки аудио файла в виде непосредственно данных в сети, вы можете изменить реализацию как вам угодно. 

Чем данный проект может быть полезен?
1. 	Многие частно забывают свои пароли, однако, теперь в качестве пароля вы можете выбрать свою любимою песню и получить его, запустив над ней этот алгоритм. 
2. 	Также, если вам необходимо сгенерировать ID (уникальных идентификаторов) для личного пользования вы можете положиться на этот алгоритм
3. 	Однако, выходные данные этого алгоритма можно использовать для шифрования трафика, сообщений или в качестве алгоритма генерации ключей для «рукопожатий» и так далее

### Реализации:
В исследовательских целях я создал 3 реализации. Две из них похоже друг на друга, а третья имеет с ними мало общего. 
#### Центральная идея: 
Прежде всего опишу реализаций Ame (Audio mapping encoder – одноимённая с названием всего проектом) и Fibuki (Functional Inheritance: Based on Universal Kernel Interfaces).
Декомпозиция в этих проектах основана на выделении трёх основных сущностей: 
- ***Reader*** – «читает» аудиофайл
- ***FourierTransform*** – проводит преобразование Фурье над «прочитанными» данными из аудиофайла 
- ***Mikuzator*** – проводит над частотным спектром преобразования, генерирую либо ID, либо пароль

Последовательно соединяя каждый из этих компонентов, мы получим конвейер, который соответствует базовым принципам SOLID, а добавив механизмы внедрения зависимостей (интерфейсы), мы получаем масштабируемую систему. 

#### Ame и Fibuki

Как раз в способе осуществления внедрения зависимостей находятся различия в Ame и Fibuki: Ame использует для концепты (*concepts*) из C++20, а Fibuki – *базовые классы с виртуальными функциями* и ожидает их наследников с override виртуальных функций.

Изначально предполагалось, что большую универсальность без оверхеда (накладных расходов) дадут концепты, однако, на практике оказалось, что использование концептов ведёт к уменьшению явности конвейера.
Экспериментальным путём установлено, что современные тулчейны (использовался GCC 13.2.0) научились почти идеально оптимизировать вызов виртуальных функций (обычно он медленный из-за вызова в виртуальную таблицу) путём девиртуализации, таким образом оверхед (накладные расходы) становятся почти нулевыми, при этом явность самого конвейера не уменьшается. В этом плане реализация Fibuki лучше.

Ещё одним отличием Ame от Fibuki является подход к использованию библиотек для чтения данных из аудиофайлов: Ame использует динамические библиотеки (.dll): sndfile (LGPL V2.1 ) и mpg123(LGPL v2.1) – обе библиотеки связываются динамически а располагаются в PLT (Program Linkage Table), что также незначительно снижает производительность (≈ 0.001%). 
Реализация Fibuki использует header-only библиотеку (дословно — «только заголовочная библиотека») dr_libs (MIT), которая включается непосредственно в код программы, тем самым единственным оверхедом является небольшое увеличение размера получаемой библиотеки(модуля пайтона).

Однако, обе реализации: Ame и Fibuki оказываются не очень эффективными, так как для генерации пароля из mp3 файла размером 10 мебибайта уходит порядка 3 секунд (Ryzen5 3600 + Win10). Такая производительность из-за алгоритмической сложности алгоритма Быстрого преобразования – nlogn, который проводит анализ над несколькими мегабайтами чисел с плавающей точкой, что и занимает большую часть времени работы (смотри прикреплённый flamegraphs)

#### Polka
Для создания более высокопроизводительной версии было изменить ключевой алгоритм – БПФ – на какой-то более легковесный алгоритм, но не менее эффективный – такая реализация названа Polka (Processing Optimization - Low-cost Key Algorithm) . Было принято использовать кастомную реализацию криптографического алгоритма SHA512 на блоках данных, обрабатываемых этим алгоритмом в многопоточной средне на основе ThreadPool.  Данный подход не использует какие-либо сторонние библиотеки и полностью самодостаточен.  Для генерации пароля из mp3 файла размером 10 мебибайта уходит порядка 3 секунд (Ryzen5 3600 + Win10) необходимо всего около 3 сотых секунды.
Логично, что в качестве алгоритма по умолчанию для коммерческих целей стоит использовать реализацию polka. Однако, ame и fibuki носят больше научный интерес.



<br>
<br>

------------

<br>
<br>



# Ame (Audio Mapping Encoder)

## 📌 Описание проекта

AME (Audio Mapping Encoder) — это кроссплатформенное приложение для анализа аудиофайлов, выполняющее быстрое преобразование Фурье (FFT) и генерацию уникального идентификатора (ID) и пароля на основе аудиоспектра. Программа поддерживает форматы WAV, MP3 и FLAC, автоматически распознаёт тип входного файла и выполняет обработку с использованием модульной архитектуры на основе концептов (concepts из C++20). Итоговое значение может быть использовано рядовым пользователем, в криптографических алгоритмах для генерации паролей или цифровых идентификаторов.

## 📂 Структура проекта
ame/<br>
├── readers/ # Чтение аудиофайлов разных форматов <br>
│   ├── wav_reader.cpp  # Обработка файлов в формате WAV<br>
│   ├── mp3_reader.cpp  # Обработка MP3-файлов<br>
│   ├── flac_reader.cpp # Обработка FLAC-файлов<br>
├── transforms/  Реализация FFT<br>
│   ├── recursive_fft.hpp # Реализация рекурсивного алгоритма БПФ (FFT)<br>
│   ├── avx256_fft.hpp    # Реализация алгоритма БПФ (FFT) с использованием AVX2<br>
├── mikuzator/ #Генерация уникального ID/пароля<br>
│   ├── custom_mikuzator.hpp   # Генерирует ID из спектра<br>
│   ├── password_mikuzator.hpp # Генерирует пароля из спектра<br>
├── bind.cpp            # Интерфейс для Python<br>
├── main.cpp            # Пример работы с библиотекой<br>
├── include/            # Заголовочные файлы<br>

Проект организован по модульному принципу (использует концепты) и включает в себя следующие ключевые компоненты:

**🔹 Чтение аудиофайлов**
- wav_reader.hpp — обработка файлов в формате WAV
- mp3_reader.hpp — обработка MP3-файлов
- flac_reader.hpp — обработка FLAC-файлов

Каждый из этих модулей реализует чтение аудиоданных и конвертацию в единый формат для дальнейшей обработки.

**🔹 Обработка данных**
- recursive_fft.hpp — реализация рекурсивного алгоритма быстрого преобразования Фурье (FFT)
- custom_mikuzator.hpp — генерация идентификатора (ID) и случайного числа на основе спектрального анализа

**🔹 Основной процессор**
- audio_processor.hpp — управляет обработкой аудиофайлов, взаимодействует с модулями чтения, анализа и генерации ID


## ⚙️ Основные принципы работы (Contrlof flow graph)


- Чтение файла

- Определяется формат входного аудиофайла (WAV, MP3, FLAC)

- Вызывается соответствующий модуль (wav_reader.h, mp3_reader.h, flac_reader.h)

- Данные загружаются в виде вектора std::vector<std::complex<double>>

- Преобразование Фурье (FFT)

- Используется рекурсивный алгоритм (Cooley-Tukey FFT)

- Разделение входного сигнала на чётные и нечётные элементы

- Применение рекурсивного FFT к подгруппам

- Объединение результатов с использованием экспоненциальных множителей (twiddle factors)

- Генерация уникального ID

- Анализ спектра: выделяются наиболее значимые частоты

- Использование этих частот для формирования числового вектора

- Применение хэширования (SHA-256) для создания финального идентификатора

- Вывод результата

- Отображение полученного ID

- Возможность сохранить ID для использования в криптографических алгоритмах

## 📥 Сборка проекта

Как и для всех других реализаций данного проекта необходимо иметь на ПК тулчейн **GCC** и **интерпретатор Python**.
Для запуска сборки необходимо запустить скрипт на Python, находящийся в корне проекта - **build_ame_module.py**
Результатом сборки будет файл, помещённый в директорию build, **ame.pyd** для Windows или **ame*.so** для Linux и других ОС.

## 🛠 Технологии

C++20 — современный стандарт языка C++
libsndfile и libmpg123 — библиотеки для работы с аудиофайлами

  
<br>
<br>
<br>
  
  
# Fibuki (Functional Inheritance: Based on Universal Kernel Interfaces)
## 📌 Описание проекта
FIBUKI — это реализация AME, использующая методы быстрого преобразования Фурье (FFT) и динамическую архитектуру с виртуальными функциями, добавляя новый уровень гибкости и расширяемости. Идейно не отличается от Ame.


## 📂 Структура проекта

fibuki/<br>
├── Readers/ # Чтение аудиофайлов разных форматов<br>
│   ├── ReaderBase.cpp # Базовый класс<br>
│   ├── WavReader.cpp<br>
│   ├── Mp3Reader.cpp<br>
│   ├── FLACReader.cpp<br>
│   ├── ReaderFactory.cpp # Фабрика<br>
├── FourierTransforms/  Реализация FFT<br>
│   ├── FourierTransformBase.cpp #Базовый класс<br>
│   ├── FFTBasic.cpp<br>
│   ├── FFT_Recursive.cpp<br>
│   ├── FFT_ExtraOptimized.cpp<br>
├── Mikuzator/ #Генерация уникального ID/пароля<br>
│   ├── IDMikuzatorBase.cpp # Базовый класс для ID<br>
│   ├── PasswordMikuzatorBase.cpp # Базовый класс для пароля<br>
│   ├── DefaultIDMikuzator.cpp<br>
│   ├── DefaultPasswordMikuzator.cpp<br>
├── bind.cpp            # Интерфейс для Python<br>
├── main.cpp            # Пример работы с библиотекой<br>
├── include/            # Заголовочные файлы<br>

## 🏗 Классы и наследование

### 1. Чтение аудиофайлов (динамический полиморфизм)
```C++
class AudioReader {
public:
    virtual std::vector<std::complex<double>>* read(const std::string& filename) = 0;
    virtual ~AudioReader() = default;
};
```
**📌 Наследники:**

*WavReader* – чтение WAV

*Mp3Reader* – чтение MP3

*FLACReader* – чтение FLAC

------------


### 2. Преобразование Фурье (стратегия вычислений)
```C++
class FFTBase {
public:
    virtual std::vector<std::complex<double>> transform(const std::vector<std::complex<double>>& data) = 0;
    virtual ~FFTBase() = default;
};
```
**📌 Наследники:**

FFT_Recursive – рекурсивная реализация FFT

FFT_ExtraOptimized – векторизованная версия с AVX2

------------


### 3. Генерация идентификатора / пароля
```C++
class Mikuzator {
public:
    virtual uint64_t generateID(const std::vector<std::complex<double>>& spectrum) = 0;
    virtual ~Mikuzator() = default;
};
```

```C++
class PasswordMikuzator {
public:
    virtual ~PasswordMikuzator() {}
    // Генерирует пароль на основе FFT-амплитуд
    virtual std::string generate(const std::vector<float>& fft_amplitudes) = 0;
};
```

**📌 Наследники:**

DefaultIDMikuzator – генерация ID на основе спектра
DefaultPasswordMikuzator – генерация пароля на основе спектра

## ⚙️ Основные принципы работы (Contrlof flow graph)

- Чтение аудиофайла

- Определяется формат файла

- Выбирается соответствующий AudioReader

- Данные загружаются в std::vector<float>

- Применение FFT

- Выбирается стратегия FFT (FFT_Recursive или FFT_ExtraOptimized)

- Выполняется преобразование

- Генерация уникального числа

- Преобразованный спектр передаётся в Mikuzator

- Вычисляется итоговый ID/пароль

## 📥 Сборка проекта

Как и для всех других реализаций данного проекта необходимо иметь на ПК тулчейн **GCC** и **интерпретатор Python**.
Для запуска сборки необходимо запустить скрипт на Python, находящийся в корне проекта - **build_fibuki_module.py**
Результатом сборки будет файл, помещённый в директорию build, **fibuki.pyd** для Windows или **fibuki*.so** для Linux и других ОС.

## 🛠 Технологии

C++20 (концепты, полиморфизм)

AVX2 (оптимизированные вычисления)


<br>
<br>
<br>
  

# Polka (Processing Optimization - Low-cost Key Algorithm)

## 📌 Описание проекта

Основным отличием данной реализации от Ame и Fibuki является полное изменение всех анализаторов, используемых для создания ID и паролей. Эти измнениения в клчювом алгоритме повлекли за собой изменеия и других модулей. Подробнее об это можете прочититать ниже.Ключевой алгоритм для максимальной оптимизации теперь имспользует алгоритм хеширования SHA-512, а также позволяет настоить управление потоками.


## 🏗 Основные компоненты
*FileReader* — отвечает за чтение файлов, загрузку данных в память и передачу их в BlockHasher.

*BlockHasher* — отвечает за блочное хеширование данных, разбивает входные файлы на блоки и передает их для обработки в SHA512ID.

*SHA512ID* — реализация хеширования с использованием алгоритма SHA-512.

*ThreadPool* — многопоточный пул, реализованный на основе std::async(), обеспечивает параллельную обработку данных.

*PasswordGenerator* — модуль для генерации случайных паролей на основе различных алгоритмов.

*FilePasswordProcessor* — управляет обработкой файлов, включая чтение, хеширование и генерацию паролей.


## ⚙️ Основные принципы работы (Contrlof flow graph)

- *Чтение данных* : FileReader загружает содержимое файла.

- *Разделение на блоки* : BlockHasher разделяет файл на небольшие части.

- *Хеширование* : каждый блок передается в SHA512ID для вычисления SHA-512.

- *Многопоточная обработка* : ThreadPool управляет параллельным выполнением задач по хэшированию блоков.

- *Генерация паролей* : PasswordGenerator создает ключи на основе хешей.

- *Формирование результата* : FilePasswordProcessor агрегирует результаты и записывает их в выходной файл.

## 📥 Сборка проекта

Как и для всех других реализаций данного проекта необходимо иметь на ПК тулчейн **GCC** и **интерпретатор Python**.
Для запуска сборки необходимо запустить скрипт на Python, находящийся в корне проекта - **build_polka_module.py**
Результатом сборки будет файл, помещённый в директорию build, **polka.pyd** для Windows или **polka*.so** для Linux и других ОС.

## 🛠 Технологии

SHA-512 — алгоритм криптографического хеширования.

Многопоточное программирование — оптимизация за счёт ThreadPool (std::thread). Также присутствует реалзация через std::async(), но не используется.

Алгоритмическая оптимизация — улучшение производительности за счет разбиения на блоки.

<br>
<br>

# 🚀 Заключение

Хоть AME и является лишь учебным проектом, но это довольно мощное и гибкое решение для анализа аудиофайлов с возможностью применения в криптографии. Кроссплатформенность, модульная архитектура и высокая скорость обработки (polka) делают его хорошим инструментом для задач, связанных с аудиоспектральным анализом.

<br>
<br>
<br>

------------
------------


I would like to give special thanks to 🔎 Amelia Watson (https://youtube.com/@watsonamelia?si=QWZmypM5B7n1Sfa3)🔍. This project became possible thanks to the ideology of Ameway: if there is no way that you like, then you can create it yourself.


------------
------------
P.S. Добавленные построенные версии библиотек посталвяются ИСКЛЮЧИТЕЛЬНО для презнтации диплома на конечном компьютере, а не для конечного пользователя. Настоятельно рекомендую простроить все зависимости под вашу систему самостоятельно


